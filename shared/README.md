# shared/

Cross-book assets and conventions that should be consistent across the arcade.

- `shared/style/`: fonts + reusable CSS patterns
- `shared/schema/`: schema patterns/versioning used by multiple books

Keep this folder generic. Book-specific rules live inside `books/<slug>/`.

