# shared/schema/

Shared schema patterns and versioning notes.

Recommended practice:
- Keep schemas small and composable.
- Version schemas explicitly (e.g. `v1/`, `v2/`).
- Books can extend shared schemas inside `books/<slug>/schema/`.

