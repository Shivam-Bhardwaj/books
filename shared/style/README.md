# shared/style/

Cross-book styling conventions (typography, layout, accessibility constraints).

If a book needs to ship fully self-contained, it can vendor the fonts/CSS into
`books/<slug>/style/` and `site/books/<slug>/style/`.

