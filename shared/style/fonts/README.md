# shared/style/fonts/

Place shared font files here when multiple books reuse the same typography.

Notes:
- Keep filenames stable once published (URLs become part of the deployed site).
- Prefer `.woff2`.

